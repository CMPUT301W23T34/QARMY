<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    android:paddingBottom="16dp"
    android:background="@drawable/pixel_camo_background"
    >

    <TextView
        android:id="@+id/welcome_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="QARMY"
        android:textSize="80sp"
        android:textColor="@color/QARMYWhite"
        />

    <EditText
        android:id="@+id/email_or_phone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="48dp"
        android:layout_below="@+id/welcome_text"
        android:hint="Email or Phone Number"
        android:inputType="text"
        android:maxLines="1"
        android:singleLine="true"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:background="@color/QARMYGrey"
        android:textColor="@color/QARMYBlack"
        />

    <EditText
        android:id="@+id/password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/email_or_phone"
        android:layout_marginTop="12dp"
        android:hint="Password"
        android:inputType="textPassword"
        android:maxLines="1"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:singleLine="true"
        android:background="@color/QARMYGrey"
        android:textColor="@color/QARMYBlack"
        />
<!--
Do we really need the login button? I've gotten rid of it
and changed the "register" button to work with the change. We can still
play around with the spacing of things though.

    <Button
        android:id="@+id/login_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_below="@+id/password"
        android:text="Log In"
        android:textColor="#050505"
        />


    <Button
        android:id="@+id/register"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_below="@+id/login_button"
        android:text="Don't have an account? Register Here"
        android:textColor="#FFFFFF"
        />
        -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/register"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_centerVertical="true"
        android:text="ENLIST"
        android:background="@color/QARMYGrey"
        android:textColor="@color/QARMYBlack"
        android:backgroundTint="@null"
        />


</RelativeLayout>
